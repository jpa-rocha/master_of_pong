ARG BASE_BACKEND_IMAGE_NAME
ARG BASE_BACKEND_IMAGE_TAG

FROM ${BASE_BACKEND_IMAGE_NAME}:${BASE_BACKEND_IMAGE_TAG}

WORKDIR /app

COPY ./dev/app .

EXPOSE 4000

CMD ["npm", "run", "start", "--port 4000"]
